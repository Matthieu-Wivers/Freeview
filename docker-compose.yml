services:
  freeview:
    container_name: freeview
    image: docker.io/${DOCKERHUB_USERNAME}/freeview:${IMAGE_TAG:-latest}
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${APP_PORT:-8080}:80"
    restart: unless-stopped